# Phase 2 Traceability (Summary)
- Protocol DTOs / codec tests -> Protocol & Interop Contract, Interop Fixture Spec
- Capability negotiation engine -> Protocol Contract (versioning + capability negotiation)
- Config signature adapters -> Signed Config Governance + Crypto Spec
- Session orchestrator service -> DDS + SAS state machine / retry / migration behavior
- Transport interfaces + deterministic fakes -> DDS + VVMP + Edge Case Catalogue
